date picker when adding an event
in the dashboard, I added an event with today's date and it did not appear in the list of close events

active projects should appear before closed projects. otherwise it should be sorted by date.